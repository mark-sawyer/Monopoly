using UnityEngine;

public class TestTokenIconSetup : MonoBehaviour {
    void Start() {
        //for (int i = 0; i < 8; i++) {
        //    transform.GetChild(i).GetComponent<TokenIcon>().adjustVisuals();
        //}
    }
}
